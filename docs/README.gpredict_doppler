This is the gpredict_doppler-write-a-block package meant as a guide to building
out-of-tree packages. To use the gpredict_doppler blocks, the Python namespaces
is in 'gpredict_doppler', which is imported as:

    import gpredict_doppler

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(gpredict_doppler)
